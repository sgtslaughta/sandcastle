apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sandcastle-root-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: sandcastle-root-ca
  secretName: sandcastle-root-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: sandcastle-ca-issuer
spec:
  ca:
    secretName: sandcastle-root-ca-secret
