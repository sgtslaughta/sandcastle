apiVersion: kagent.dev/v1alpha2
kind: ModelConfig
metadata:
  name: gemma
  namespace: kagent
spec:
  provider: OpenAI
  model: gemma
  apiKeySecret: kagent-openai
  apiKeySecretKey: OPENAI_API_KEY
  openAI:
    baseUrl: "http://llm-d.llm-d.svc.cluster.local:8000/v1"

